import React from 'react';

import { SvgIconProps } from '@mui/material';
import VideoCameraFrontIcon from '@mui/icons-material/VideoCameraFront';

export interface SCVideoCameraFrontIconProps extends SvgIconProps {}

export const SCVideoCameraFrontIcon = (props: SCVideoCameraFrontIconProps) => (
  <VideoCameraFrontIcon {...props} />
);
